#wap in python to show the demo of the if block 


#vote eligibility 
age = eval(input('Enter the age:'))

if age>=18:
    print('Eligible for voting')

#dis-advantage if execute does not execute, 
# no output.
# so we must a add one statement which will execute agar if does not execute.
